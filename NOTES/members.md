# NOTES — members app

Location: `back-end/members/`

What exists:
- `models.py`, `views.py`, `admin.py` — skeleton present.

Checks:
- Verify `Member` model relations to `auth.User` (OneToOne) in [back-end/members/models.py].
- Add registration API / form and validation (tessera uniqueness).
