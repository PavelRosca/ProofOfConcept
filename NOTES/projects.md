# NOTES — projects app

Location: `back-end/projects/`

What exists:
- `models.py`, `views.py`, `admin.py` — placeholder for political projects per region/sector.

Action items:
- Ensure Project model has M2M to Sector and budget fields typed as Decimal with proper precision.
- Add list/detail API endpoints.
